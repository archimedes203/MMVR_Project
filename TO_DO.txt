Suggested TO-DO list: 

1. [DONE] Test-run entire code (using small number of epochs) to verify that .py file structure is understood
   and that it runs cleanly and completely in its current format.

2. Correct current model so that "precision" does not always come out to exactly 1 (seems wrong)

3. [DONE] Modify data pre-processing code to use P2S1 splitting protocol (larger training set). --> DONE (going back to P1S1)

4. Modify config.py so that training parameters can be tuned (using a grid-search method?)

5. Modify loss.py so that loss function parameters (weights) can be tuned. 
   ---> It makes sense to penalize the mean average error on L_coord more harshly (in other words, we probably want λ_coord > λ_hm), 
        because we want the final keypoint guesses to be "more correct" than the heatmap approximiations. But there is still wiggle room 
        to play with these weights. If we have enough time, we can do a "coarse grid search" where we try values for λ_hm and λ_coord over 
        a larger range at bigger step sizes, then see which one gives the smallest losses. Then, we do a "refined grid search," i.e. we 
        "zoom in" on the optimal coarse-search values and scan over an even tighter interval around each. That will take time, but could 
        increase model accuracy.

6. Make sure keypoints are not all being "stacked" onto a single point in example results.
